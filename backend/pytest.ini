[pytest]
testpaths = tests
norecursedirs = .* build dist CVS _darcs {arch} *.egg e2e
markers =
    e2e: production or cross-service smoke tests (set E2E_RUN=1 to enable)
    playwright: browser tests via Playwright (set E2E_RUN_PLAYWRIGHT=1 to enable)
